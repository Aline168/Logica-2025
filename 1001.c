#include <stdio.h>

int main() {
int A, B, X;

// Lê dois inteiros da entrada
scanf("%d", &A);
scanf("%d", &B);

// Soma os dois valores
X = A + B;

// Imprime o resultado no formato exigido
printf("X = %d\n", X);

return 0;
}
